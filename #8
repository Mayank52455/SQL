SELECT *
FROM patients
WHERE first_name LIKE '__r%'         -- 3rd letter must be 'r'
  AND gender = 'F'
  AND MONTH(birth_date) IN (2, 5, 12)
  AND weight BETWEEN 60 AND 80
  AND patient_id % 2 != 0
  AND city = 'Kingston';
